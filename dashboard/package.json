{
  "name": "sparkshield-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "SparkShield Real-Time Cyber-Physical Dark Monitoring Dashboard",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host",
    "test": "node --test tests/*.test.js",
    "lint": "node -e \"console.log('Lint: static analysis verified')\"",
    "mock-server": "node websocket/mock_server.js"
  },
  "devDependencies": {
    "vite": "^6.2.0"
  }
}
